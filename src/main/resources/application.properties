# Application Name
spring.application.name=asset-management-system

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/mecon?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Rounak@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA (Hibernate) Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server Port (Optional - default is 8080)
server.port=8080

# Logging (Optional - for better debugging)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

# (Optional) CORS, if your frontend is separate
# spring.web.cors.allowed-origins=http://localhost:3000
